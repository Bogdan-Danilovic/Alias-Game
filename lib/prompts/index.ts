// Re-export alias words — this file exists only for standalone dev.
// In Game Hub, the add-game script copies alias.ts into the shared lib/prompts/ folder.
export { getRandomWords, ALIAS_CATEGORIES } from './alias';
